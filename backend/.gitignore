# backend/reference/ ist explizit committet (Snapshot der sovaia-contracts-
# Reference). Wird mit `make sync-reference` aktualisiert. Bei Reference-
# Änderungen: sync + commit + push triggert CI + Image-Bump + Pod-Restart.
